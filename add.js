/**
 * Just a simple function to add two numbers together.
 *
 * @param {a} first number
 * @param {b} second number
 */
module.exports = function add(a, b) {
  throw new Error('This is the code that breaks add function.');
};
